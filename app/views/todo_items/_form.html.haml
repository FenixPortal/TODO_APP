= form_for [@todo_list, @todo_list.todo_items.build], {:html => { :class => "form-horizontal" }} do |f|

  = f.text_field :content, :input_html => { :class => 'autogrow form-control', :rows => 10, :cols => 120, :maxlength => 120  }, placeholder: "New Todo", label: "content", error: 'Content is mandatory, please specify the content'
  =f.submit